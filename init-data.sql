-- Create the WT_users database if it doesn't already exist
CREATE DATABASE IF NOT EXISTS WT_users;

-- Create the WT_customers database if it doesn't already exist
CREATE DATABASE IF NOT EXISTS WT_customers;